# Yet Another Simple Site Blocker

A Firefox browser extension for blocking web pages. Has three features:

- Block entire websites by domain
- Block pages by keyword (page blocked if keyword appears in page title)
- Allow specific pages from a blocked domain

Currently supports Firefox only.
